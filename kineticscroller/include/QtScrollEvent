#include <QtGlobal>
#if (QT_VERSION >= QT_VERSION_CHECK(4, 9, 0))
#  error "Please do not use the QtScroller solution with Qt > 4.8 - use the internal QScroller class instead"
#endif
#include "../src/qtscrollevent.h"
